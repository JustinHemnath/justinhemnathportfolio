export enum SECTIONS {
  ABOUT = "about",
  PROJECTS = "projects",
  CONTACT = "contact",
}

export enum PROJECTS {
  CHAT_APP = "Chat App",
}

export const CHAT_APP_EVENTS = {
  TO_SERVER: "TO_SERVER",
  TO_CLIENT: "TO_CLIENT",
};
